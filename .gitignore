# Secrets — never commit
.env

# Old iterations and hardware test scripts (contain credentials/API keys)
backup/
templates/index.new

# Database (large, contains logged data)
*.db

# Python
venv/
__pycache__/
*.pyc

# Editor/OS
.DS_Store
*~

# Local Claude Code settings
.claude/settings.local.json
